export const PLAY_SCENE = {
  SCENE: {},
  setScene: function (scene) {
    this.SCENE = scene;
  },
};

export const HUD_SCENE = {
  SCENE: {},
  setScene: function (scene) {
    this.SCENE = scene;
  },
};

export const LOADING_SCENE = {
  SCENE: {},
  setScene: function (scene) {
    this.SCENE = scene;
  },
};

export const BOOTSTRAP_SCENE = {
  SCENE: {},
  setScene: function (scene) {
    this.SCENE = scene;
  },
};
